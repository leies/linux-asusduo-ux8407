# UX8407AA: PM: suspend entry (s2idle) and (deep) both never return.
# Refuse system suspend so a menu/sleep key cannot hard-hang the machine.
[Sleep]
AllowSuspend=no
AllowSuspendThenHibernate=no
AllowHybridSleep=no
